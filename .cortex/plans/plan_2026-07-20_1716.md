---
created: 2026-07-20T17:16:31.180Z
session: 7def8165-cf5c-4e8b-9cea-68e016480f17
working_directory: /home/clever/dev/snowflake/snowflake-examples
---

## Context

The repo is early-stage with one example (`mcp/create-mcp-agent.sql`). Convention: self-contained files with a header block explaining purpose, prerequisites, and usage. The README lists planned directories including `sql/` and `streamlit/`.

## Implementation Steps

### Example 1: Cortex AI Functions Demo (`sql/cortex-ai-functions.sql`)

Create a single SQL script that:
1. Creates a sample `product_reviews` table with ~10 rows of realistic review data
2. Demonstrates each Cortex AI function on that data:
   - `SNOWFLAKE.CORTEX.COMPLETE()` — generate a response/summary
   - `SNOWFLAKE.CORTEX.SUMMARIZE()` — summarize long reviews
   - `SNOWFLAKE.CORTEX.SENTIMENT()` — score review sentiment
   - `SNOWFLAKE.CORTEX.TRANSLATE()` — translate a review to another language
   - `SNOWFLAKE.CORTEX.EXTRACT_ANSWER()` — extract specific info from text
3. Follows the existing style: header block with description/prerequisites/usage, parameterized config section, step-by-step sections with comments
4. Uses `CREATE OR REPLACE TEMPORARY TABLE` so it's non-destructive

### Example 2: Cortex Agent Chat Interface (`streamlit/cortex-agent-chat/`)

Create a Streamlit in Snowflake app with:
1. `streamlit/cortex-agent-chat/app.py` — Main Streamlit app:
   - Chat UI using `st.chat_message` / `st.chat_input`
   - Calls an existing Cortex Agent via the REST API (`_snowflake.send_snow_api_request` or `snowflake.connector` session)
   - Maintains conversation history in `st.session_state`
   - Configuration sidebar for selecting agent name, database, schema
2. `streamlit/cortex-agent-chat/environment.yml` — Dependencies (snowflake-snowpark-python, streamlit)
3. `streamlit/cortex-agent-chat/README.md` — Setup instructions (how to deploy via `CREATE STREAMLIT`, prerequisites like having a Cortex Agent already created)

## Verification

- **SQL example**: Can be run directly in a Snowflake worksheet — each query should return results independently. No external dependencies beyond Cortex AI being enabled on the account.
- **Streamlit app**: Deploy via `CREATE STREAMLIT` or Snowflake CLI and test the chat UI against the Qlik MCP agent created by the other example.

## Critical Files

- `mcp/create-mcp-agent.sql` — Pattern to follow for SQL file structure/style
- `README.md` — May need minor update to reflect new content
- `sql/cortex-ai-functions.sql` — New file: Cortex AI functions demo
- `streamlit/cortex-agent-chat/app.py` — New file: Streamlit chat app
- `streamlit/cortex-agent-chat/README.md` — New file: Setup docs for the Streamlit app