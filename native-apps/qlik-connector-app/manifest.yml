manifest_version: 1

version:
  name: "1.0"
  label: "Qlik Connector App v1.0"
  comment: "Native App template for Qlik + Snowflake integrations"

artifacts:
  setup_script: setup_script.sql

references:
  - consumer_warehouse:
      label: "Warehouse"
      description: "Warehouse for agent and query execution"
      privileges:
        - USAGE
      object_type: WAREHOUSE
      register_callback: core.register_reference
